# 云开发部署指南

第一次部署需要在微信开发者工具里手动操作，以后修改代码只需要重新上传云函数。

## 环境信息

- **AppID**：`wx9a6f67961fd0b631`
- **云开发环境 ID**：`cloud1-d5gapkl6615f7b76a`

---

## Step 1：在云开发控制台创建数据库集合

打开微信开发者工具 → 顶部点 **「☁️ 云开发」** → 进入云开发控制台 → 左侧 **「数据库」**

点击 **「+ 添加集合」** 创建以下两个集合：

### 集合 1：`books`（书籍表）

字段会在第一次写入时自动创建，**不需要手动建字段**。

### 集合 2：`users`（用户表）

同上，自动建字段。

---

## Step 2：设置数据库权限

进入每个集合的 **「权限设置」**：

| 集合 | 权限 |
|---|---|
| `books` | **「所有用户可读，仅创建者可读写」**（让任何用户能浏览，但只能改自己发的书） |
| `users` | **「仅创建者可读写」**（保护隐私） |

> 写操作我们都通过云函数走（云函数有管理员权限，会绕过这些规则），所以这里设严格点没关系。

---

## Step 3：上传并部署 5 个云函数

回到开发者工具 → 左侧文件树展开 `cloudfunctions/` → 你会看到 7 个文件夹：

```
cloudfunctions/
├── login/
├── publishBook/
├── updateBook/        ← 新增
├── deleteBook/        ← 新增
├── reserveBook/
├── confirmHandover/
└── releaseExpired/
```

**对每一个**云函数执行：

1. **右键** 该文件夹
2. 选 **「在云端安装依赖（不上传代码）」**（第一次需要，安装 wx-server-sdk）
3. 等几秒，看到 ✅ 安装完成提示
4. 再次右键 → **「上传并部署：云端安装依赖（不上传 node_modules）」**
5. 等待上传完成（每个 30 秒～1 分钟）

> 5 个函数全部部署完，约 5-8 分钟。可以多窗口并行操作。

---

## Step 4：配置定时任务（释放超时预约）

`releaseExpired` 这个函数我已经在 `cloudfunctions/releaseExpired/config.json` 里配好了 cron：

```json
"config": "0 0 * * * * *"
```

这是**每小时整点跑一次**。上传部署后，定时器会自动生效，无需额外配置。

> 验证方式：进入云开发控制台 → 云函数 → releaseExpired → 触发器，能看到这条触发器记录。

---

## Step 5：开通云存储（用于书籍封面）

云存储默认随云开发开通，无需额外操作。但要检查权限：

1. 云开发控制台 → 左侧 **「存储」** → **「权限设置」**
2. 选 **「所有用户可读，仅创建者可读写」**

---

## Step 6：第一次跑通

1. 编译运行小程序（点工具左上角 **「编译」**）
2. 进入「我的」Tab → 个人设置 → 填写昵称、微信号、城市 → 保存
3. 回到书架 → 点右下角 + 发书 → 拍封面、填表 → 发布
4. 回到书架，应该能看到自己刚发的书 ✅
5. 用另一个微信号扫码（在工具里点 **「预览」** 生成二维码）→ 申请这本书 → 看到对方微信号弹窗

---

## 常见问题

### Q1：云函数报错「Permission denied」
A：检查数据库权限是否设置正确，特别是 `books` 集合要让所有用户可读。

### Q2：上传云函数失败「Cannot find module 'wx-server-sdk'」
A：先执行「在云端安装依赖」，再上传部署。

### Q3：图片显示不出来（cloud://...）
A：云存储里的图片用 `<image src="{{cloudPath}}">` 直接渲染需要先用 `wx.cloud.getTempFileURL()` 转成临时 URL，或者直接放云存储 ID 也能渲染（小程序内可以）。

### Q4：定时任务没触发
A：腾讯云函数定时器**最快每分钟一次**，每小时一次的延迟正常。可以在控制台手动「测试」一次验证逻辑。

---

## 修改代码后的更新流程

| 改了什么 | 怎么更新 |
|---|---|
| 前端代码（pages/） | 工具会自动热重载，无需操作 |
| 云函数代码 | 右键对应函数文件夹 → 「上传并部署」 |
| 云函数依赖（package.json） | 右键 → 「在云端安装依赖」+ 「上传并部署」|
| 数据库结构 | 直接在控制台改集合或权限 |
| 定时任务 cron | 改 config.json 后重新上传函数 |
